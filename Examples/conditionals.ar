name = true
thing = false
println(
    name
)

if (name) println("1. YES")
else println("1. NO")

if(thing) {
    println("2. NO")
} else {
    println("2. YES")
}

println( if (name) "3. YES")
println( if (thing) "4. NO" else "Hell yes")

a = 0
while (a < 5) {
    println(a)
    a = a +1
}